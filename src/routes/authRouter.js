const { Router } = require('express')
const router = Router()

router.post('/register', )
router.post('/login', )
router.post('/token', )
router.post('/forgotten-password', )
router.post('/users', ) // admin route

module.exports = router